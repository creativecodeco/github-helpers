PORT=3000
# Optional: Personal Access Token (PAT) or GITHUB_TOKEN to increase API rate limit (60 requests/hr -> 5000 requests/hr)
# and query private repo metadata or exact user details if needed.
GITHUB_TOKEN=
# Key to protect access to the /api/metrics and /api/metrics/users endpoints
METRICS_KEY=
